# `components/ai` — AI components (Family 08)

AI-specific presentational components (streaming, citations, trust affordances). Composes `foundation` + `ui`; keeps AI content visibly distinct from source (02.6 AD-5). No feature logic.
