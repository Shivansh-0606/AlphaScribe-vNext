# `lib/state` — Shared client (UI) state

Small, cross-cutting Zustand stores for genuinely global UI state (e.g. companion visibility) — 03.1. Feature-local state lives in the feature's `application/` layer. Never mirrors server data (03.1 AD-5).
