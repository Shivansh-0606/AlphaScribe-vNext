# `lib/config` — Typed configuration

Centralized, typed, validated configuration consumed through one contract (05.4 AD-6). `env.ts` enforces the absolute public/secret boundary (05.4 AD-2) and fails fast on invalid config (05.4 AD-5).
