# `tests` — Test infrastructure

`setup/` — Vitest setup + shared test utilities/render helpers. `e2e/` — Playwright journey + accessibility tests. Unit/component tests are co-located with source as `*.test.ts(x)` (05.1 AD-3).
