# `providers` — Global React providers

App-wide client providers composed once at the root (Phase 3): Query client, Theme (single frozen light theme — no toggle, 04.3), Toaster, and the error/suspense boundaries wiring. Thin composition only.
