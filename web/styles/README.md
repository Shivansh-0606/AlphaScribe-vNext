# `styles` — Global styles & token layer

Global CSS, the Tailwind v4 `@theme` token declarations (Tier-2, generated/single-sourced from the frozen Design Tokens — 04.2), and font wiring (IBM Plex). **The only place raw design values live** (04.2 AD-3).
