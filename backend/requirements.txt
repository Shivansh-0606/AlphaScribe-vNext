fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
pymongo==4.6.3
motor==3.3.1
pydantic>=2.6.4
numpy>=1.26.0
python-multipart>=0.0.9
requests>=2.31.0
pytest>=8.0.0
pytest-xdist>=3.6.0
pytest-cov>=5.0.0     # 06 §0.1 approved stack — dev/CI only; coverage baseline (M2 Ph0)

# --- LLM providers (bring-your-own key) ---
# gemini via google SDK; openai SDK covers openai/groq/openrouter/deepseek/
# mistral/custom (OpenAI-compatible); anthropic via its own SDK.
google-generativeai>=0.8.0
openai>=1.40.0
anthropic>=0.34.0

# --- global company data (non-US fallback when SEC EDGAR has nothing) ---
yfinance>=0.2.40

# --- PDF ingest (annual reports, esp. Indian NSE/BSE filings) ---
pypdf>=4.0.0

# --- imported by the app but were missing from the original manifest ---
httpx>=0.27.0
langgraph>=0.2.0
rank-bm25>=0.2.2
fastembed>=0.3.6

# --- M2 Phase 1 shared infrastructure (06 §0.1 approved stack) ---
pydantic-settings>=2.2      # app/settings.py — centralized config (06 AD-9)
redis>=5.0                  # infrastructure/redis/ — 09 job lifecycle/events/rate-limit
opentelemetry-sdk>=1.25
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-pymongo>=0.46b0
opentelemetry-instrumentation-httpx>=0.46b0
opentelemetry-instrumentation-redis>=0.46b0   # M6 — same approved OTel family, covers infrastructure/redis/
opentelemetry-exporter-otlp-proto-http>=1.25
prometheus-client>=0.20
